export const init ={
    isLogIn: false,    // true or false data stored in this variable
    user:'',           //refresh also page keep login that user and token get from session store
    rowdata:{},          // it is for whenever click update open model card that model need row data from this. 
    isShowModel:false  // whenever click on update then isShowModel is true then open Model otherwise not open Model
}